import React from "react"
import FriendData from "./FriendData"
import Friend from "./Friend"
import Pet from "./Pet"

function FriendList() {
   // const FriendComponents=FriendData.map(friend=> <Friend Name={friend.name} Age = {friend.age}/>)
   // const PetComponents=FriendData.map(friend=> <Pet PetName={friend.pets.name}/>)
return (
    <div>
        {/* {FriendComponents} */}
        {/* {PetComponents} */}
    </div>
)
}

export default FriendList